# Issue tracker

This project uses GitHub Issues at `wenbo-wei/codex-dashboard` after
publication.

When a commit references `#123`, retrieve it with:

```sh
gh issue view 123 --repo wenbo-wei/codex-dashboard
```

For the initial release, the originating specification is
`docs/requirements.md`.

